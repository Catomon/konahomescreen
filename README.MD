# Compose Multiplatform Application targeting only Windows.

Run application: `./gradlew :composeApp:run`
Run **hot reload** application: `./gradlew :composeApp:runHot`)
Run UI tests: `./gradlew :composeApp:jvmTest`
